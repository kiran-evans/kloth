export const AccountPopover = () => {
    return (
        <div id="accountPopover">
            {/* If user is logged in, show Orders, Account Settings, Logout */}
            {/* If user is not logged in, show button to either login or create account */}
        </div>
    )
}